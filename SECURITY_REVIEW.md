# LineMemo Lite 사내 설치 보안 검토

검토일: 2026-05-23

## 결론

사내 설치는 가능하지만, 현재 빌드는 로컬 평문 JSON 저장 앱이므로 회사 보안 기준상 "반복 문구/템플릿/비민감 복사값" 용도로 제한하는 것이 안전합니다. 운영 비밀번호, API 키, 개인정보, 고객정보를 저장해야 한다면 배포 전에 암호화 저장과 배포 서명 정책을 먼저 보강해야 합니다.

## 런타임 동작 범위

- 데이터 저장 위치: 기본값은 `%APPDATA%\LineMemoLite\data.json`이며, 사용자가 설정에서 폴더 또는 `.json` 파일 경로로 변경할 수 있습니다. 사용자 지정 경로 포인터는 `%APPDATA%\LineMemoLite\storage.json`에 저장됩니다.
- 백업 위치: `%APPDATA%\LineMemoLite\backup\`
- 네트워크 통신: 앱 자체의 동기화/원격 전송 로직은 없습니다.
- 외부 열기: 사용자가 URL 줄의 "링크 열기"를 누르면 기본 브라우저로 열립니다.
- 클립보드: 복사 기능을 위해 클립보드 쓰기를 사용하고, 자동 삭제가 켜져 있으면 현재 클립보드를 읽어 앱이 복사한 값과 같은 경우만 비웁니다.
- 알림: 유효기간 만료/임박 항목을 OS 알림으로 표시합니다. 알림 본문에는 복사값 대신 카드명, 라벨, 날짜만 표시합니다.
- 자동 실행: 설정에서 켜는 경우에만 Windows 시작 시 실행 등록을 시도합니다.

## 보안 프로그램에서 걸릴 수 있는 지점

- 미서명 실행 파일/설치 파일: SmartScreen, EDR, 백신, AppLocker, WDAC에서 "알 수 없는 게시자" 또는 평판 부족으로 차단될 수 있습니다.
- NSIS/MSI 설치: 사내 정책이 사용자 설치, MSI 실행, `%ProgramFiles%` 또는 사용자 프로필 설치를 제한하면 배포가 막힐 수 있습니다.
- 클립보드 접근: DLP/EDR이 클립보드 읽기/쓰기 앱을 민감 동작으로 분류할 수 있습니다.
- 자동 실행 등록: 시작프로그램/Run 등록이 보안 정책에 의해 차단되거나 경고될 수 있습니다.
- 로컬 평문 저장: 기본 또는 사용자 지정 위치의 JSON과 백업 파일이 암호화되지 않아, 파일 접근 권한이 있는 사용자/프로세스가 내용을 읽을 수 있습니다.
- WebView2 런타임: 사내 PC에 Microsoft Edge WebView2 Runtime이 없고 설치가 제한되어 있으면 앱 실행이 실패할 수 있습니다.
- OS 알림: Windows 토스트 알림은 설치된 앱에서 정상 동작합니다. 개발 실행에서는 표시 이름/아이콘이 다르게 보일 수 있습니다.

## 배포 전 권장 조치

- 코드 서명 인증서로 `.exe`, NSIS, MSI 산출물을 서명합니다.
- 보안팀에 실행 파일명, 설치 경로, 기본 데이터 경로, 사용자 지정 저장 위치 허용 범위, SHA-256 해시, 게시자 인증서 정보를 전달해 허용 목록을 준비합니다.
- 사내 배포는 MSI 중심으로 검토하고, Intune/SCCM/그룹 정책 배포 절차에 맞춥니다.
- WebView2 Runtime 사전 설치 여부를 확인합니다.
- 기본값처럼 `launchOnStartup`, `expiryNotifications`, `autoClearClipboard`는 사용자가 선택할 때만 켜지게 유지합니다.
- 운영 비밀값 저장이 필요하면 Windows DPAPI 또는 별도 암호화 저장소를 도입한 뒤 배포합니다.
- DLP 환경에서는 알림 본문과 클립보드 자동 삭제 정책을 사내 기준에 맞춰 검토합니다.

## 현재 기본값

- 자동 실행: 꺼짐
- 유효기간 알림: 꺼짐
- 클립보드 자동 삭제: 꺼짐
- 앱 잠금: 꺼짐
- 저장 파일 암호화: 없음
